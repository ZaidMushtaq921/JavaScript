const userId = 123;
let name = "John Doe";
var age = 25; // var is a function scoped variable, let and const are block
gender = "male"; // not recommended as it will be global if not in strict mode

console.log(userId);
console.table([name, age, gender]);
